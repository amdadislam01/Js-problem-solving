/**
 * Take four parameters. Multiply the four numbers and then return the result
 */

function multiplyFourNumbers(a, b, c, d) {
  let result = a * b * c * d;
  return result;
}

console.log(multiplyFourNumbers(5, 4, 2, 6)); 

/**
 * Output : 240
 */